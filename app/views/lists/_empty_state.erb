<div id="<%= status %>_empty" class="flex flex-col h-full w-full text-light-gray-2">
    <span><%= status == "archived" ? "Nenhuma lista arquivada." : "Você ainda não tem nenhuma lista." %></span>
</div>
